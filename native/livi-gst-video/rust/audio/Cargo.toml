[package]
name = "livi-audio-stream"
version = "0.1.0"
edition.workspace = true

# Framing and decryption of a CarPlay audio stream.
[lib]
crate-type = ["rlib"]

[dependencies]
livi-crypto-node = { path = "../../../livi-crypto/rust", default-features = false }

[target.'cfg(target_os = "linux")'.dependencies]
glib = "0.21"
socket2 = "0.6"
